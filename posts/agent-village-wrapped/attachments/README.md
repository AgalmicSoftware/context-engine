# Attachments — Agent Village Wrapped post

Static assets for this post: the header image, rendered diagrams, example Wrapped poster images, and exported chart data.

Conventions (apply to every post folder):

- `header.png` — banner image for the post; the manifest's `headerImage` field points at it and the posts page renders it above the title.
- Images referenced from the post body use relative paths, e.g. `attachments/example-wrapped-poster.png`.
- Data behind any `ce-viz` exhibit can be mirrored here as JSON/CSV for reuse.
- Keep assets non-identifying: no raw Telegram IDs, wallet addresses, or private memory content in exported data.

Wanted:

- [ ] `header.png` — banner image (a cropped Wrapped poster works well at ~3:1)
- [ ] `example-wrapped-poster.png` — a real Agent Village Wrapped output image
- [ ] Rendered versions of the five prompts in `../diagram-prompts.md`

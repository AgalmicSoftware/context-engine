# Attachments — Agent Village Wrapped post

Static assets for this post: rendered diagrams, example Wrapped poster images, and exported chart data.

Conventions:

- Images referenced from the post use relative paths, e.g. `attachments/example-wrapped-poster.png`.
- Data behind any `ce-viz` exhibit can be mirrored here as JSON/CSV for reuse.
- Keep assets non-identifying: no raw Telegram IDs, wallet addresses, or private memory content in exported data.

Wanted:

- [ ] `example-wrapped-poster.png` — a real Agent Village Wrapped output image
- [ ] Rendered versions of the five prompts in `../diagram-prompts.md`

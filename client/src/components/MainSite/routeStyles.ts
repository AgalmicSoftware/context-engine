/**
 * @module components/MainSite/routeStyles
 */

export const ROUTE_STATUS_SHELL_STYLE = Object.freeze({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  minHeight: '58vh',
  padding: '2rem 1.25rem',
  color: '#f4f7ff',
});

export const ROUTE_STATUS_CARD_STYLE = Object.freeze({
  width: 'min(100%, 42rem)',
  padding: '1.75rem',
  borderRadius: '1.25rem',
  border: '1px solid rgba(90,255,194,0.24)',
  background: 'linear-gradient(180deg, rgba(18,24,35,0.96) 0%, rgba(10,14,22,0.94) 100%)',
  boxShadow: '0 18px 45px rgba(0,0,0,0.28)',
});

export const ROUTE_STATUS_EYEBROW_STYLE = Object.freeze({
  marginBottom: '0.75rem',
  fontSize: '0.78rem',
  fontWeight: 700,
  letterSpacing: '0.12em',
  textTransform: 'uppercase',
  color: 'rgba(90,255,194,0.88)',
});

export const ROUTE_STATUS_TITLE_STYLE = Object.freeze({
  margin: 0,
  fontSize: '1.9rem',
  fontWeight: 700,
  color: '#f4f7ff',
});

export const ROUTE_STATUS_BODY_STYLE = Object.freeze({
  marginTop: '0.85rem',
  marginBottom: 0,
  fontSize: '1rem',
  lineHeight: 1.6,
  color: 'rgba(244,247,255,0.76)',
});

export const ROUTE_STATUS_PATH_STYLE = Object.freeze({
  marginTop: '0.9rem',
  marginBottom: 0,
  fontSize: '0.94rem',
  color: 'rgba(244,247,255,0.6)',
});

export const ROUTE_STATUS_LINK_STYLE = Object.freeze({
  display: 'inline-flex',
  alignItems: 'center',
  justifyContent: 'center',
  marginTop: '1.25rem',
  padding: '0.8rem 1.05rem',
  borderRadius: '999px',
  border: '1px solid rgba(90,255,194,0.32)',
  color: '#5affc2',
  textDecoration: 'none',
  fontWeight: 700,
});
